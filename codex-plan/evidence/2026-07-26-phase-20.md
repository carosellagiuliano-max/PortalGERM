# Evidence — Phase 20 Identität, E-Mail und zuverlässige Benachrichtigungen

> **STATUS: TECHNISCH ABGESCHLOSSEN UND VERIFIZIERT.** Der unveränderliche
> Candidate `59089009f54312a4c10989b7efde2d5fda9a2b8d` besteht den
> Phase-20-Identity-/Notification-Vertrag und das vollständige G3:
> 45 Migrationen, deterministischer Seed×2, 1.984 Unit-, 408 PostgreSQL- und
> 233 Production-Browsertests sowie Build, HTTP und HSTS. Reale Zustellung,
> autonomes Scheduling, Production-Replay und MFA/Step-up sind dadurch
> ausdrücklich nicht aktiviert.

## Identität und Statusgrenze

| Feld | Wert |
| --- | --- |
| Datum | 26. Juli 2026, Europe/Berlin (UTC+02:00) |
| Phase | 20 — Identität, E-Mail und zuverlässige Benachrichtigungen |
| Arbeitsbranch | `codex/phase-20-identity-email-notifications` |
| Verifizierter Candidate-Commit | `59089009f54312a4c10989b7efde2d5fda9a2b8d` |
| Candidate-Parent | `8087c0ca1f17d5244f2781f839040c6a9bf16afd` |
| Commit-Identität | `Giuliano Carosella <carosellagiuliano@gmail.com>` |
| Planstatus | abgeschlossen |
| Technikstatus | technisch abgeschlossen |
| Quality-Gate | bestanden |
| Aktivierung | Adapter höchstens `SANDBOX`; Runtime standardmässig `DISABLED`/`PAUSED`; `LIVE` deaktiviert |
| Zulässige Aussage | „Identity-/Outbox-/Dispatcher-/Preference-Vertrag ist lokal und in isoliertem PostgreSQL technisch verifiziert“ |
| Nicht zulässige Aussage | keine reale Zustellung, Inboxplatzierung, Provider-/DNS-/DPA-Freigabe, autonome Worker-, MFA-, Pilot- oder Production-Reife |

Dieser Record ist ein nachfolgender reiner Evidence-/Status-Close-out. Alle
Runtime-, Migrations-, Unit-, PostgreSQL-, Build-, HTTP-, HSTS- und
Browserresultate beziehen sich auf den oben genannten Candidate. Der
Close-out verändert keinen Produkt- oder Testcode.

## Umgebung

| Komponente | Verifizierter Wert |
| --- | --- |
| Betriebssystem | Windows `win32-x64` |
| Node.js | `v24.18.0` |
| npm | `11.16.0` |
| Git | `2.51.0.windows.1` |
| Docker Engine | `29.5.2` |
| Docker Compose | `v5.1.3` |
| PostgreSQL | Image/Tools `16.13` |
| Next.js | `16.2.11` |
| Prisma | `7.8.0` |
| Playwright | `1.61.1` |
| Browser-Netzwerkpolicy | loopback-only |

Test- und Seed-Datenbanken wurden isoliert erzeugt und nach dem Lauf
entfernt. Es wurden keine vollständigen Datenbank-URLs, E-Mail-Adressen,
Tokens, Providersecrets oder Payloadinhalte in diesen Record übernommen.

## Umgesetzter Scope

- `LOW_ASSURANCE`, `VERIFIED_EMAIL` und konservatives
  `LEGACY_ASSURANCE`; keine stille LIVE-Verifikation;
- Candidate-, Employer- und Invitation-Registrierung mit atomarem Challenge-
  und Outboxwrite;
- gehashte, single-use, zeitgebundene, supersedable Verification Tokens mit
  exakt einer Wirkung bei konkurrierendem Consume;
- sicherer Login-E-Mail-Wechsel mit Pending-Adresse, Address Epoch,
  Sessionrotation/-revocation und Pflichtnotice an die alte Adresse;
- typisierte, deduplizierte Notification-Outbox mit maximal 32 KiB,
  verschlüsseltem externem Delivery-Material, Attempts, Lease/Heartbeat,
  Providerdeadline, Retry/Backoff, Suppression, DLQ und lokal auditiertem
  Replay;
- geschlossene Purpose-/Template-/Preference-Matrix; Pflichtkommunikation
  bleibt unabhängig von optionalem Marketing-/Job-Alert-Opt-out;
- fail-closed Providerkomposition mit `disabled`, `local_mock` und einem nur
  für `@resend.dev` zulässigen Resend-Sandboxadapter ohne Real→Mock-Fallback;
- `/verify-email`, Candidate-/Employer-Preference-Center und redigierte
  Admin-Systemübersicht;
- serverseitige Low-Assurance-Gates für Candidate-, Employer-, Privacy-,
  Team-, Application-, Saved-Job- und Radar-Writes.

Das zugehörige versiegelte Inventar steht in
[`phase20-identity-notification-inventory.md`](../phase20-identity-notification-inventory.md).

## Exakte Golden- und Failure-Gates

| Befehl / Gate | Ergebnis | Beobachtung |
| --- | --- | --- |
| `git status --short`; `git rev-parse HEAD` | bestanden, Exit `0` | sauberer Candidate `59089009…` |
| `npm run env:validate` | bestanden, Exit `0` | Schema, Keylängen, Rotationsversionen und sichere Runtime-Gates valide |
| `npm run db:generate` | bestanden, Exit `0` | Prisma Client 7.8.0 erzeugt |
| `npm run db:validate` | bestanden, Exit `0` | Prisma-Schema valide |
| `npm run db:migrate`; `npm run db:migrate:status` | bestanden, Exit `0` | 45 Migrationen, keine ausstehende Migration |
| isolierte Migration + Seed A + Seed B + Read-only-Verifikation | bestanden, Exit `0` | je 738 Checks; erster Lauf neu, zweiter completed; Manifest A=B=Verify |
| `npm run lint` | bestanden, Exit `0` | gesamtes Repository ohne ESLint-Fehler |
| `npm run typecheck` | bestanden, Exit `0` | Next-Typegen und TypeScript ohne Fehler |
| `npm test` | bestanden, Exit `0` | 249/249 Dateien, **1.984/1.984 Unit-Tests**, 197,18 s |
| `npm run test:integration` | bestanden, Exit `0` | 84/84 Dateien, **408/408 PostgreSQL-Tests**, 533,24 s |
| `npm run build` | bestanden, Exit `0` | Production-Build mit 103 Seiten und 7 Handlern |
| `npm run test:e2e:http` | bestanden, Exit `0` | Public/Auth/Private, CSP, Privacy, Health, robots/sitemap und Header |
| `npm run test:e2e:browser` | bestanden, Exit `0` | **233/233**, 0 fail/skip/timeout/interrupted, Retry `0`, 15,6 min |
| `npm run test:e2e:hsts` | bestanden, Exit `0` | exakter HSTS-Header; Loopback belegt nicht die reale TLS-Kette |
| `npx tsx --conditions react-server scripts/phase20-email-provider-smoke.ts --mode=sandbox --scenario=deliver,duplicate,bounce,429,500,timeout,bad-config` | erwarteter Exit `1` | alle sieben Szenarien `PASS`; Gesamtfehler nur wegen absichtlich ungültiger Konfiguration; `externalNetwork:false` |
| `npm run notifications:dispatch -- --batch=1 --worker-id=phase20-evidence` | bestanden, Exit `0` | Provider `disabled-v1`, Zustand `PAUSED`, keine erfundene Zustellung |
| `npm run notifications:dispatch -- --batch=0` | erwarteter Exit `1` | redigiertes `NOTIFICATION_DISPATCH_FAILED`, kein Payload-/Secret-Leak |
| `npm run route:audit` | bestanden, Exit `0` | 103 Seiten, 7 Handler; Rollenlayouts und Inventar konsistent |
| `npm run security:release-scan` | bestanden, Exit `0` | 1.194 tracked Dateien; keine Schlüssel, Backups, Provider-Tokens oder exakten Secrets |
| `npm run license:audit` | technisch bestanden, Exit `0` | 910 Pakete, 0 verboten, 29 Review-Items, 1 akzeptiertes Missing-Metadata-Item |

Auf dem anschliessenden reinen Evidence-/Status-Close-out liefen
`npm run plan:audit`, `npm run route:audit`,
`npm run security:release-scan`, `npm run license:audit` und
`git diff --check` erneut mit Exit `0`. Der aktualisierte Planaudit zählt
20 abgeschlossene Phasen, 1.093 geprüfte Punkte in 20 Dateien und 417
auflösbare lokale Markdown-Links. Der Close-out verändert keinen
Produkt-, Migrations- oder Testcode und wird nicht in die Candidate-
Testzahlen eingerechnet.

Der reproduzierbare isolierte Seednachweis lief mit:

```powershell
npx tsx --conditions react-server -e 'import { createMigratedTestDatabase } from "./tests/fixtures/isolated-postgres.ts"; import { createDatabaseClient } from "./lib/db/factory.ts"; import { orchestrateDemoSeed, verifyPersistedDemoSeed } from "./prisma/seed/orchestrator.ts"; void (async () => { const migrated = await createMigratedTestDatabase("phase20_evidence_seed"); const database = createDatabaseClient(migrated.connectionString); try { const first = await orchestrateDemoSeed(database); const second = await orchestrateDemoSeed(database); const verified = await verifyPersistedDemoSeed(database); console.log(JSON.stringify({ firstPreviouslyCompleted: first.previouslyCompleted, secondPreviouslyCompleted: second.previouslyCompleted, firstChecks: first.verificationCheckCount, secondChecks: second.verificationCheckCount, readOnlyChecks: verified.verificationCheckCount, manifestStable: first.envelope.manifestSha256 === second.envelope.manifestSha256 && second.envelope.manifestSha256 === verified.envelope.manifestSha256 })); } finally { await database.$disconnect(); await migrated.dispose(); } })();'
```

Resultat:
`{"firstPreviouslyCompleted":false,"secondPreviouslyCompleted":true,"firstChecks":738,"secondChecks":738,"readOnlyChecks":738,"manifestStable":true}`.

## Browser- und Route-Evidence

| Feld | Wert |
| --- | --- |
| Manifest | `test-results/phase17/run-manifest.json` |
| Manifest-SHA-256 | `91607e9a3792c1067809b57052f7b80b8d334c2751fbd187290bdb5791a9d0a0` |
| Candidate im Manifest | `59089009f54312a4c10989b7efde2d5fda9a2b8d` |
| Lauf | `2026-07-26T14:46:10.881Z` bis `2026-07-26T15:01:47.317Z` |
| Projekte | `chromium-journeys`, `chromium-mobile-360` |
| Tests | 233 passed, 0 failed, 0 skipped, 0 timed out, 0 interrupted |
| Retry | `0` |
| Inventar | 103 Seiten, 7 Handler |
| Phase-20-Deep-Flows | Registration→Verify→Privacy→Preferences→E-Mail-Change; Employer Preferences→enumeration-sicherer Resend |
| Phase-20-Quality | Missing/Invalid/Resend, Candidate Preferences und redigierte Adminzustände jeweils Desktop + 360 px |

Die Qualitätsprüfungen blockieren horizontales Clipping, kritische/ernste
Axe-Befunde, Hydration-/Consolefehler und nicht erreichbare erlaubte
Interaktionen. Sie sind keine moderierte Nutzerforschung; diese bleibt Phase
29.

## Datenbank-, Race-, Queue- und Redaction-Evidence

| Vertrag | Nachweis |
| --- | --- |
| Migrationen | 45 vollständig deploybar; zwei additive Phase-20-Migrationen |
| Audit-Actions | 129 zwischen Typed Constant, Prisma und Coverage-Matrix synchron |
| Tokenrace | 20 konkurrierende Consumes ergeben exakt 1 Verify-Wirkung und 19 sichere Konflikte |
| Dispatcher-Concurrency | 105 Messages, zwei Worker, Batchlimit 100; 105 Claims/Deliveries/Attempts, keine doppelte fachliche Wirkung |
| Lease/Crash | abgelaufene Lease wird übernommen; stabiler Provider-Dedupe-Key bleibt erhalten |
| Retry/DLQ | 429/5xx/Timeout transient; permanente/Poison-Fälle bounded; Max Attempts endet in DLQ |
| Payloadgrenze | Oversize, URL- und secret-/bearer-förmige Payloads werden vor Persistenz abgewiesen |
| Preferences | optionaler Opt-out vor Dispatch suppressiert; Pflichtzwecke bleiben zustellbar |
| Replay | Local/Mock/Admin/auditiert; Production ohne Phase-25-Step-up fail-closed |
| Adminsicht | nur redigierte Counts/Zustände, keine Adresse, Token, Payload oder Providersecret |

Die lokale Referenz belegt funktionale Bounds und Concurrency, aber kein
Produktions-Latenz-/Durchsatz-SLO. Reale 50-Request-p95-, Providerlatenz-,
Backlog- und Capacity-Schwellen werden vor Aktivierung in Phase 23 auf der
Zielinfrastruktur gemessen.

## Akzeptanzkriterien

| AC | Ergebnis | Beleg |
| --- | --- | --- |
| `20-AC-01` | PASS | Candidate-/Employer-/Invitation-Low-Assurance und atomare Verify-Outbox |
| `20-AC-02` | PASS | TTL, Resend, Supersession, single-use und 20-Client-Race |
| `20-AC-03` | PASS | Pending-Adresse, Epochwechsel, Sessionrevocation und Altadress-Notice |
| `20-AC-04` | PASS | 105-Message-Concurrency, Lease, Retry, Crash, DLQ und Replay-Denial |
| `20-AC-05` | PASS | geschlossene Purpose-/Preference-Matrix, Pflicht-/Optional-Trennung |
| `20-AC-06` | PASS | Registration→Verify→Privacy und vier Denialklassen |
| `20-AC-07` | PASS | Rate-/Signal-Matrix, Enumeration-Schutz und Capability-Denial |
| `20-AC-08` | PASS technisch | Provider Contract und deterministischer Sandbox-Failure-Smoke; reale Vendoraktivierung offen |
| `20-AC-09` | PASS | Desktop/360 px, Keyboard-/Fokuszustände, Axe critical/serious `0` |
| `20-AC-10` | PASS | leerer/Legacy-/Wiederholungs-/Parallelwriter-Migrationsvertrag |
| `20-AC-11` | PASS | vollständiges G3 auf Candidate `5908900`, Retry/Skip `0` |

## Nicht als Gate gezählte Diagnose- und Korrekturläufe

| Beobachtung | Bewertung und Korrektur |
| --- | --- |
| Der erste vollständige Unit-Lauf fand vier vor Phase 20 gepinnte Verträge: 122 statt 129 Audit-Actions, alte letzte Migration, alte Phase-16-Matrix und pauschales Provider-Netzwerkverbot. | Die Verträge wurden auf die sieben neuen Actions, Migration 45 und den eng allowlisteten Resend-Sandboxadapter aktualisiert. Danach liefen 1.984 Tests auf dem Candidate ohne Retry. |
| Der erste vollständige PostgreSQL-Lauf fand die alte Migrationsliste und einen Phase-11-Test mit fester Fachuhr, aber Seed auf realer Uhr. | Die zwei Migrationen wurden ergänzt; der Testseed wurde deterministisch auf seine Fachuhr gesetzt. Der finale Candidate-Lauf bestand 408/408. |
| Die lokale langjährig genutzte Demo-DB enthielt einen bereits bestehenden, vom Manifest abweichenden Job und verweigerte den Seed korrekt. | Es wurde nichts überschrieben. Der verbindliche Nachweis lief auf einer frischen isolierten DB: Migration 45, Seed×2 und 738 Read-only-Checks grün. |
| Ein früher Provider-Smoke ohne `--conditions react-server` endete am `server-only`-Import statt am beabsichtigten Negativszenario. | Der exakte dokumentierte Befehl enthält die Bedingung; alle sieben Szenarien sind `PASS`, der erwartete Exit `1` stammt ausschliesslich aus `bad-config`. |

Keiner dieser Diagnoseversuche wird als erfolgreicher Gate-Lauf gezählt. Es
wurde kein roter Lauf als Pass umgedeutet und kein Test entfernt, geskippt
oder mit Retry kaschiert.

## Offene Grenzen und externe Gates

- `EMAIL_PROVIDER_MODE=disabled`,
  `NOTIFICATION_DISPATCH=paused`, `OPTIONAL_EMAIL=false` und
  `DELIVERY_REPLAY=false` bleiben sichere Defaults.
- Der Resend-Adapter ist technisch vorhanden, aber es gab keine reale
  Netzwerkzustellung, kein Vendorreceipt, keine Inboxplatzierungsprüfung und
  keine SPF-/DKIM-/DMARC-, DPA-, Datenregions- oder Reputationsfreigabe.
- Autonomer Scheduler, Pager, Production-Backlog-/Bounce-/DLQ-SLO,
  Retention-Jobs und Provider-Aktivierungsledger bleiben Phase 23.
- Production-Replay, frische risikobasierte Step-up-Evidence, MFA und
  Recovery-/Supportpolitik bleiben Phase 25.
- Phase 22 besitzt weiterhin die vollständige Retention-, Export-, Erasure-,
  Legal-Hold- und Consent-Freigabe für die neuen Tabellen.
- Der technische License-Scan ist keine Rechtsfreigabe.

## Go/No-go

**GO für Phase 21** als nächsten sequenziellen technischen Track auf Basis
des Candidate-Commits. Phase 21 darf die verifizierte Identity und die
versionierten Notification-Hooks übernehmen, aber keine reale Zustellung
aktivieren.

**NO-GO für reale E-Mail-Zustellung, unbeaufsichtigte Worker, Production-
Replay sowie jede Pilot-, Public-, Paid-, Scale- oder Production-Freigabe.**
Diese Gates bleiben bei Phase 22/23/25 und den benannten externen Ownern.
