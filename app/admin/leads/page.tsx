import type { Metadata } from "next"; import { LeadList } from "@/components/admin/LeadList"; import { requireAdminPage } from "@/lib/auth/route-guards"; import { listAdminLeads } from "@/lib/admin/leads"; import { getDatabase } from "@/lib/db/client";
export const metadata: Metadata = { title: "Sales Leads" };
export default async function AdminLeadsPage() { const admin = await requireAdminPage(); const now = new Date(); const dependencies = { actor: { userId: admin.id, email: admin.email, role: admin.role, status: admin.status }, correlationId: "admin-leads-read", database: getDatabase(), now } as const; const leads = await listAdminLeads(dependencies) ?? []; return <div className="grid gap-6"><header><p className="eyebrow">Sales Operations</p><h1 className="mt-2 text-3xl font-semibold">Leads</h1><p className="mt-2 text-muted-foreground">Neue und zugewiesene Leads werden nach dem 24-Stunden-Ziel geordnet; Überfälligkeit ist sichtbar.</p></header><LeadList leads={leads} now={now} /></div>; }
