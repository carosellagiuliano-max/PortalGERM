-- Phase 01 baseline: deliberately no domain tables.
-- Applying this migration creates Prisma migration history and proves the
-- release migration path without pretending that Phase 02 is implemented.
SELECT 1;
