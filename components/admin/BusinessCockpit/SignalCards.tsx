import { Badge } from "@/components/ui/badge"; import type { AdminCockpitSignal } from "@/lib/admin/cockpit";
export function SignalCards({ signals }: Readonly<{ signals: readonly AdminCockpitSignal[] }>) { return signals.length === 0 ? <p className="rounded-lg border border-dashed p-8 text-center text-sm text-muted-foreground">Keine belastbaren Signale. Fehlende Analytics-Samples erzeugen bewusst keine spekulativen Karten.</p> : <div className="grid gap-4 lg:grid-cols-2">{signals.map((signal, index) => <article key={`${signal.reason}-${signal.companyId}-${signal.jobId ?? index}`} className="rounded-xl border bg-card p-5"><div className="flex flex-wrap gap-2"><Badge variant="secondary">{signal.reason}</Badge>{signal.dataProvenance === "DEMO" ? <Badge variant="outline">Demo-Evidenz</Badge> : null}</div><h3 className="mt-3 font-semibold">{signal.title}</h3><p className="mt-2 text-sm text-muted-foreground">{signal.evidence}</p><p className="mt-4 text-sm"><strong>Vorschlag:</strong> {signal.suggestedAction}</p></article>)}</div>; }
