import { randomUUID } from "node:crypto"; import { AdminActionForm, adminInputClass, adminTextareaClass } from "@/components/admin/action-form";
export function ContentEditor() { return <AdminActionForm operation="content-draft" label="Sicheren Entwurf anlegen" hidden={{ idempotencyKey: randomUUID() }}><div className="grid gap-3 sm:grid-cols-2"><label className="grid gap-1 text-sm">Typ<select name="type" className={adminInputClass} defaultValue="GUIDE"><option>GUIDE</option><option>CLUSTER</option></select></label><label className="grid gap-1 text-sm">Locale<input name="locale" defaultValue="de-CH" className={adminInputClass} /></label></div><label className="grid gap-1 text-sm">Slug<input name="slug" required className={adminInputClass} /></label><label className="grid gap-1 text-sm">Canonical Path<input name="canonicalPath" placeholder="/guide/mein-ratgeber" required className={adminInputClass} /></label><label className="grid gap-1 text-sm">Titel<input name="title" required className={adminInputClass} /></label><label className="grid gap-1 text-sm">Kurztext<textarea name="excerpt" required className={adminTextareaClass} /></label><label className="grid gap-1 text-sm">Markdown (sichere Allowlist)<textarea name="body" required className={adminTextareaClass} rows={10} /></label></AdminActionForm>; }
