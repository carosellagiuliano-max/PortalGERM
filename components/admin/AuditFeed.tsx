import { Badge } from "@/components/ui/badge";
import { formatDateTime } from "@/lib/utils/format";
export function AuditFeed({ entries }: Readonly<{ entries: readonly Readonly<{ id: string; action: string; targetType: string; result: string; reasonCode: string | null; createdAt: Date }>[] }>) { return entries.length === 0 ? <p className="text-sm text-muted-foreground">Noch keine Admin-Aktionen vorhanden.</p> : <ol className="divide-y">{entries.map((entry) => <li key={entry.id} className="grid gap-1 py-3 sm:grid-cols-[1fr_auto]"><div><p className="font-medium">{entry.action.replaceAll("_", " ")}</p><p className="text-xs text-muted-foreground">{entry.targetType} · {entry.reasonCode ?? "ohne Reason-Code"}</p></div><div className="flex items-center gap-2"><Badge variant={entry.result === "SUCCEEDED" ? "secondary" : "destructive"}>{entry.result}</Badge><time className="text-xs text-muted-foreground">{formatDateTime(entry.createdAt)}</time></div></li>)}</ol>; }
